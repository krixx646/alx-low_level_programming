include "main.h"
/**
 * _islower-check if character is lowercase.
 * @c:character checker || pointer
 * Return:0- true 1 false.
 */
int
_islower(int c)
{
	if (j >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
