#include <stdio.h>
/**
 * main : A program that stars tge process, its also a function
 * return: 0, a program that ends the process
 **/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
